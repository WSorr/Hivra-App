[package]
name = "hivra-engine"
version = "1.0.0"
edition = "2021"
authors = ["Hivra Team"]
license = "MIT"
description = "Orchestration layer for Hivra protocol"

[dependencies]
hivra-core = { path = "../../core/hivra-core" }

[features]
default = ["std"]
std = []
