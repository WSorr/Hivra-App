[package]
name = "hivra-wasm-runtime"
version = "1.0.0"
edition = "2021"
description = "Deterministic sandboxed WASM runtime for Hivra plugin ABI"

[dependencies]
wasmi = "=0.38.0"

[dev-dependencies]
wat = "1"
